# Python dependencies for the POC
pyserial
python-dotenv
openai

# Backend server (agent/server.py) -- HTTP + WebSocket bridge to the
# liquid-gas dashboard frontend, per BACKEND_INTEGRATION_CONTRACT.md
fastapi
uvicorn

# Architecture B only (Temporaral_Context_Approach/telemetry_service.py) --
# MQTT subscriber for the asynchronous sensor telemetry stream. The existing
# synchronous architecture does not import this.
paho-mqtt>=2.0
